const {notificacionRepository:r}=require('../repositories/notificacionRepository');const {AppError}=require('../utils/appError');const notificacionService={async listar(userId,f){await r.generateAlerts();return r.findByUser(userId,f)},async leer(id,userId){const n=await r.markRead(id,userId);if(!n)throw new AppError('Notificación no encontrada',404);return n}};module.exports={notificacionService};
